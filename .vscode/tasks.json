{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/httpd/"
            }
        }
    ],
    "version": "2.0.0"
}